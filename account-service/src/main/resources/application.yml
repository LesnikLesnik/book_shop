spring:
  application:
    name: account-service
  config:
    import: optional:configserver:http://config-service:8888
  cloud:
    config:
      fail-fast: true
      username: user
      password: admin